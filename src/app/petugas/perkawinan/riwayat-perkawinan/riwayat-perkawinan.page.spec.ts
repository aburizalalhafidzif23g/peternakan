import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RiwayatPerkawinanPage } from './riwayat-perkawinan.page';

describe('RiwayatPerkawinanPage', () => {
  let component: RiwayatPerkawinanPage;
  let fixture: ComponentFixture<RiwayatPerkawinanPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RiwayatPerkawinanPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
