import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DetailRiwayatPerkawinanPage } from './detail-riwayat-perkawinan.page';

describe('DetailRiwayatPerkawinanPage', () => {
  let component: DetailRiwayatPerkawinanPage;
  let fixture: ComponentFixture<DetailRiwayatPerkawinanPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DetailRiwayatPerkawinanPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
